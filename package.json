{
  "name": "mahjong-mcr",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "validation"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@mahjong-mcr/ui",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present && npm run test:e2e --workspace=@mahjong-mcr/ui --if-present"
  }
}
